{
  "name": "react-demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5",
    "spanan": "^1.2.0",
    "cliqz-home-benchmark": "file:../../cliqz-home-benchmark"
  },
  "scripts": {
    "build": "rm -rf build dist; react-scripts build; mv build dist; echo ignore errors, it is all good",
    "serve": "npm run build && cliqz-serve; mv build dist"
  }
}
